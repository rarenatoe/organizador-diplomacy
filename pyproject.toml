[project]
name = "organizador-diplomacy"
version = "0.1.0"
description = "Organizador de partidas para la liga de Diplomacy.Ñol"
requires-python = ">=3.13"
dependencies = [
    "notion-client>=3.0.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[project.scripts]
organizar   = "organizador:organizar_partidas"
notion-sync = "notion_sync:main"
