# ── Credenciales (NUNCA al repo) ──────────────────────────────────────────────
.env

# ── Archivos generados por el organizador ─────────────────────────────────────
# Los jugadores_NNNN.csv SE TRACKEAN (historial inmutable de la liga).
reporte_*.txt
# metadata.json tracks runtime state (pending badge + sync lineage).
# Excluded because it changes on every sync/organizar run.
data/metadata.json
openapi.json

# ── SQLite database (runtime state) ──────────────────────────────────────────
data/diplomacy.db
data/diplomacy.db-shm
data/diplomacy.db-wal

# ── Python / uv ───────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.python-version

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Node / TypeScript build (bun) ────────────────────────────────────────────
node_modules/
# bun.lockb is committed (reproducible installs).
# frontend/static/app.js is a build artifact — regenerate with: bun run build
frontend/static/app.js
frontend/static/index.css
frontend/static/index.html

.aider*
